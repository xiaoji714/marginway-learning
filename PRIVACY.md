# 隐私说明

语境没有开发者托管后端、遥测或账户系统。

- 资源网址、原文、词汇、笔记、复习及 Agent 来源存储于本机 SQLite。数据库默认目录为 `~/.local/share/learning-companion`，由本机用户访问。
- API Key 只保存在当前 Chrome profile 的扩展 storage.local，限受信扩展上下文读取，不写入 SQLite、Prompt 或备份。
- 字幕获取向 Supadata 发送规范化视频网址，不发送播放列表和追踪参数，仅请求原生字幕。
- 翻译向 DeepSeek 发送所选文本、原文语境、资源标题；不主动发送整库笔记。
- 「在 Agent 中讨论」将简短交接指令、材料节选与记录 ID 复制到剪贴板；粘贴到其他服务是用户后续操作。
- CLI 身份为本机客户端自报，标记 Agent 生成，不代表第三方身份认证。CLI 权限等同当前系统用户。

卸载扩展不会自动删除 SQLite 学习记录。用户可以通过资料库导出记录，也可自行删除本机数据目录。删除浏览器扩展会按 Chrome 行为清除该扩展配置；先导出需要保留的资料。
